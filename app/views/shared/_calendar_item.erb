<% if item.class == CategoryNote %>
  <%= link_to item.title, category_note_path(item), data: {turbo_frame: "_top"} %>
<% elsif item.class == CategoryChecklist %>
  <%= link_to item.title, category_checklist_path(item), data: {turbo_frame: "_top"} %>
<% elsif item.class == TaskNote %>
  <%= link_to item.title, task_note_path(item), data: {turbo_frame: "_top"} %>
<% elsif item.class == TaskChecklist %>
  <%= link_to item.title, task_checklist_path(item), data: {turbo_frame: "_top"} %>
<% end %>
